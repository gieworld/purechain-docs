name = "purechain-docs-chat"
main = "worker.js"
compatibility_date = "2026-01-01"

# 20 questions per minute per IP.
[[unsafe.bindings]]
name = "RATE_LIMITER"
type = "ratelimit"
namespace_id = "1001"
simple = { limit = 20, period = 60 }
